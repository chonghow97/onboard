import React from "react";
import { <%= props.name %>Props } from "./props";
import { Container } from "./styles";

// More complex reusable components and hooks
// calls reusable hooks/functionws from `./hooks`
export const <%= props.name %> = (props: <%= props.name %>Props) => {
  // const {} = props
  // --------------------STATES & VARIABLES

  // --------------------HOOKS

  // --------------------FUNCTIONS

  // --------------------RENDER
  return <Container />;
};
