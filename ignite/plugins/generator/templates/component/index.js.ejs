import React from "react";
import { <%= props.name %>Props } from "./props";
import { Container } from "./styles";

// Simple reusable component, handles simple ui or functions
export const <%= props.name %> = (props: <%= props.name %>Props) => {
  // const {} = props
  // --------------------STATES & VARIABLES

  // --------------------HOOKS

  // --------------------FUNCTIONS

  // --------------------RENDER
  return <Container />;
};
