import React from "react";
import { <%= props.pascalCaseName %>Props } from "./props";
import { Container } from "./styles";

// Simple reusable component, handles simple ui or functions
export const <%= props.pascalCaseName %> = (props: <%= props.pascalCaseName %>Props) => {
  const { ...restProps } = props;
  // --------------------STATES & VARIABLES

  // --------------------HOOKS

  // --------------------FUNCTIONS

  // --------------------RENDER
  return <Container {...restProps} />;
};

<%= props.pascalCaseName %>.defaultProps = {};
